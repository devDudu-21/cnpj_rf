# Configurações do banco de dados PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dados_cnpj

# URL completa (opcional, sobrescreve as configurações acima se presente)
# DATABASE_URL=postgresql://usuario:senha@host:porta/banco
